import tkinter as tk
root = tk.Tk()
root.title(“label configuration example”)
root.geometry(“300x200”) # set window size

label = tk.Label(root,
text=”hello, tkinter!”,
bg=”red”,
fg=”white”, 
font=(“times”, 18, “bold”), 
padx=10, 
pady=10, 
relief=”solid”) #

label.pack(pady=20) 

root.mainloop()
